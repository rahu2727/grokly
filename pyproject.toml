[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "grokly"
version = "0.1.0"
description = "AI knowledge assistant for enterprise systems"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["grokly*"]
